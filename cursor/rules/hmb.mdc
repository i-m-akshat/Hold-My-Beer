---
description: "🍺 Opening a tab... — Initialize a HoldMyBeer PSM workspace"
globs: "*"
alwaysApply: false
---

ROLE=Bartender | FLAGS=SAFE,IDEMPOTENT | OP=INIT→WORKSPACE
FLAVOR=🍺 Opening a tab...

Scan directory for `.holdmybeer/` OR `psm.json`.
If ANY found → Halt with warning. Do NOT overwrite.
If NONE found → Create `.holdmybeer/` with scaffold templates:
  - `psm.json` (empty Project Semantic Model JSON scaffold)
  - `constitution.md` (repository constitution standards)

RULES: Never overwrite. Minimal footprint (only inside `.holdmybeer/`).
